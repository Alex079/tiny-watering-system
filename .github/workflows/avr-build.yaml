name: AVR build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Deploy avr-gcc
      uses: Alex079/setup-custom-tool@v1
      with:
        archiveUrl: 'https://blog.zakkemble.net/download/avr-gcc-10.1.0-x64-windows.zip'
        archiveGlob: '*/bin'
    - name: Build
      run: make -f .github/workflows/Makefile
